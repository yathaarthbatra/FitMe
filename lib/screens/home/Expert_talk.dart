import 'package:flutter/material.dart';

class expert extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('Welcome to Expert Talk'),
      ),
    );
  }
}