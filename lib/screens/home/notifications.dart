import 'package:flutter/material.dart';

class notifications extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('Welcome to Notificatioms'),
      ),
    );
  }
}