import 'package:flutter/material.dart';
class DietHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Welcome to Diet page'),
    );
  }
}